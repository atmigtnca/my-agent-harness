# my-agent-harness — 에이전트 하네스

<!-- /wiki-start 시공 시: 아래 문단을 프로젝트 소개로 교체한다 (무엇에 관한 위키인지, 원본이 무엇인지). -->
이 repo는 팀과 AI 에이전트가 공유하는 **에이전트 하네스**다 — 기억(위키)·무인 구현 루프·Advisor/Worker 위임이 한 몸이다.
그 기반은 세션 간 지식 저장소다.
에이전트의 세션은 휘발되지만, 세션에서 얻은 지식(삽질의 해결책·사고·결정·개념)은
여기 `wiki/`에 남아 다음 세션과 다음 사람의 시간을 아낀다.

## 구조

```
my-agent-harness/
├── AGENTS.md              # 이 파일 — 모든 AI 에이전트용 공통 컨텍스트 (원본)
├── CLAUDE.md              # → AGENTS.md symlink
├── README.md              # 사람이 읽는 첫 문서 — 템플릿 적용법·커스터마이즈 포인트
├── wiki/                  # 지식 저장소 본체 — 규칙은 wiki/SCHEMA.md, 색인은 wiki/index.md (섹션은 /wiki-start 시공이 결정)
├── docs/loop/             # loop 운영 규약 — loop-guide.md(사이클 루틴 원본) + state-files.md(상태 파일 스키마)
├── docs/harness-guide.md  # 하네스 운영 상세 — Advisor/Worker 규약 원본·공용과 개인·온보딩·거버넌스(운영 모드)
├── docs/agents-guide.md   # 스킬·서브에이전트 작성 규약 — 본문 골격·tools allowlist·정본+래퍼
├── docs/publish-slot.md   # 외부 발행 파이프라인을 꽂는 자리의 규약
├── scripts/               # 위키 자동화(lint·sync)·loop-lint·skill-lint·githooks, 온보딩 setup.sh
├── .claude/               # Claude Code: 공유 settings.json(훅·권한), 스킬(wiki 4종·loop-plan·handoff), 서브에이전트 3종(reviewer·wiki-scout·wiki-auditor)
├── .agents/               # AGENTS.md 규약 런타임: .claude 정본을 가리키는 얇은 스킬 래퍼
└── .github/               # CI — wiki-lint·skill-lint·loop-lint·비밀 검출 (로컬 훅의 최후 방어선)
```

이 밖에 gitignore된 로컬 상태(`.handoff/` 인계문 등)가 루트에 보일 수 있다 —
repo 구조의 일부가 아니니 지도에 없어도 놀라지 마라.

**이 트리가 곧 구조 지도다. 세션 시작 시 "구조 파악" 탐색(find/ls 훑기)을 다시 하지 마라** —
이 문서를 전제로 필요한 파일만 곧장 열어라. 위키 규칙 → `wiki/SCHEMA.md`,
위키 내용 → `wiki/index.md`, loop 규약 → `docs/loop/`, 위임·거버넌스 상세 → `docs/harness-guide.md`.
이 문서와 실제 구조가 다르면 탐색으로 때우지 말고 이 문서를 고쳐라.

## 에이전트 행동 수칙

이 문서에서 하나만 기억해야 한다면 이 절이다.

1. **위키가 시공 전이면** (`wiki/SCHEMA.md` frontmatter `config.sections`가 비어 있음)
   다른 위키 작업 전에 `/wiki-start` 시공을 사용자에게 제안하라 — 시공 전에는 문서를
   기록할 수 없다 (wiki-lint가 막는다).
2. **같은 에러·증상에 2회 이상 막히면** 계속 파기 전에 `grep -ri "<키워드·에러문자열>" wiki/`.
   훑기는 `wiki/index.md` — 어떤 지식이 어느 섹션에 사는지는 SCHEMA "프로젝트 규약"이 정의한다.
   **탐색 라우팅**: 단건 조회(색인·grep 한 번 적중)는 직접 읽고, 여러 문서에 걸친 **종합** 질문·2회 막힌 뒤
   광역 탐색·작업 전 위키 **브리핑**은 `wiki-scout`에 위임한다 — 열람은 그쪽이 지고 너는 결론과 원문 발췌만 받는다.
3. **`verified`(검증됨)만 신뢰하라.** `draft`(초안)는 참고만. 본문 첫 줄에 만료 배너가 있는
   문서는 `sources`를 직접 대조한 후에만 근거로 쓴다. 위키↔코드 불일치 시 **코드가 맞다** —
   발견하면 문서를 고치고 SCHEMA의 기록 경로를 따른다.
4. **기록 트리거에 해당하는 지식이 생기면** (30분+ 삽질 / 반복 질문 / 사고·장애 /
   되돌리기 어려운 결정 / AGENTS.md 심화) 메모해 두었다가 **진행 중인 작업이 마무리된
   시점에** 제안하라 — 작업 흐름을 끊지 않는다. **"마무리" 판정은 에이전트가 하지 않는다** —
   사용자가 화제를 닫거나 세션 정리를 말할 때만 제안하고, 그전엔 조용히 메모만 해둔다
   (자동 수거는 없다 — 세션이 닫히기 전에는 반드시 제안해 기록까지 마쳐라).
   절차는 `wiki/SCHEMA.md`의 "기록 경로"가 정의한다 — 승인 없이 `verified`는 없고,
   사용자가 실시간 응답할 수 없는 세션에서는 묻지 말고 `draft`로.
5. **위키 기록과 외부 발행 제안이 함께 걸리면** 제안·승인 타이밍은 `docs/publish-slot.md`가 원본이다.
6. **트리거 밖 지식은 기록하지 마라.** "그래도 유용할 듯"이 위키를 죽인다 —
   필터 없는 축적은 노이즈다 (전체 ~50문서 목표).
7. **세션은 이 repo 루트에서 시작하는 것이 기본이다.** 하위 디렉토리에서 시작하면
   공유 설정·스킬·훅이 로드되지 않는다.
8. **새 구현 작업은 크기 무관 `/loop-plan` → 내장 `/loop` 경로가 기본이다.** 스펙·상태
   파일을 `docs/projects/<slug>/`에 시공하고 무인 사이클로 굴린다 — 상세는 아래
   "loop — 사용 요약". 예외는 한두 줄 수정처럼 시공 오버헤드가 더 큰 경우뿐.
9. **스킬 실행은 정규형으로 선언한다.** 스킬을 발동할 때 안내 문구는 "<스킬명> 프로토콜 발동합니다"로 말한다(예: "loop-plan 프로토콜 발동합니다").

## 모델 역할 분담: Advisor / Worker — 요약

원본: `docs/harness-guide.md` (위임 목록·브리프 기준·병렬 격리·경계·감독 규율 전문).

- 메인 세션은 Advisor(판단: 분해·설계·브리프·검증·커밋 승인·보고), 구현 노동 전부는
  Worker(서브에이전트, Advisor 한 티어 아래 최신 모델)에게 위임한다. Worker는 커밋하지 않는다.
- **작성자≠채점자**: Worker의 완료 보고를 믿지 말고 Advisor가 diff·검증을 직접 재실행해
  통과분만 승인한다. 검증 실패는 재위임 — 직접 수정은 한두 줄 마무리뿐.
- 커밋 전 fresh-context 리뷰의 기본 도구는 `reviewer` 서브에이전트다.
- **진행 중인 Worker에 개입하지 마라** — 개입은 증거 기반 4조건(블로커 보고 / 작업 소진 /
  반복 실패+구체 교정 / 명백한 이탈)에서만. 위임 후 완료 기준을 올리지 않는다.
- 새 서브에이전트 작성은 `docs/agents-guide.md` 규약을 따른다.

## loop — 사용 요약

구현 작업을 무인 사이클로 굴리는 규약이다. **루프는 하나** — 작업 크기와 무관하게 같은 구조,
분량만 비례한다. 원본은 `docs/loop/loop-guide.md`(사이클 루틴·검증 사다리·함정 목록)와
`docs/loop/state-files.md`(상태 파일 스키마).

- 새 구현 작업 = `/loop-plan`으로 `docs/projects/<slug>/`에 스펙 SSOT(requirements.md)와
  상태 파일 2종(features.json·progress.md)을 시공 → 사용자만 풀 수 있는 blocker 소진 →
  Claude Code 내장 `/loop`(주기 재실행)으로 사이클을 굴린다.
- 정지 조건: 해당 프로젝트 features.json **전 항목 판정 완료** — 판정 필드와 수정 권한 규칙은 `docs/loop/state-files.md` §2·§3이 원본이다.
- 사이클마다 통과분만 커밋하고(push는 안 한다) progress.md에 다음 wakeup 인계를 남긴다.

## 위키 — 사용 요약

깊은 맥락(아키텍처, 운영 절차, 과거 결정, 트러블슈팅 사례)이 필요하면 `wiki/index.md`부터 읽어라.
전체 규칙은 `wiki/SCHEMA.md`가 정의한다 — **기록하기 전에 반드시 읽을 것.**
여기 없는 규칙은 규칙이 아니며, 이 문서와 충돌하면 SCHEMA가 이긴다.

- 최초 1회 `/wiki-start`가 프로젝트 상황을 인터뷰해 운영 모드·섹션·태그·프로젝트 규약을 시공한다.
- 기록의 경로는 **세션 중 확인** 하나뿐이다: 본문 전체를 사용자에게 보여주고 승인받아
  `verified`로 커밋 (`/wiki-record` 스킬이 세션 말 회고·기록을 안내한다).
  자동 수거 백스톱은 없다 — 세션이 끝나기 전에 기록하지 않으면 그 지식은 사라진다.
- 기록 전 검산: 이 repo를 겪지 않은 유능한 에이전트도 **이미 아는** 지식이면 기록하지 않는다 (SCHEMA "기록 트리거").
- `verified` 문서를 수정하면 status를 **draft로 되돌린다** (에이전트 포함, 예외 없음 — SCHEMA "라이프사이클").
- 에이전트는 위키 문서를 **직접 삭제**할 수 없다 — 경고 삽입 + draft 강등까지만, 삭제 확정은 사람이 한다 (SCHEMA "삭제").
- 잔여 draft·만료 문서는 책임자가 분기마다 큐레이션한다 (`/wiki-curate` 스킬).
- 위키 밖에서 작성된 기존 문서 뭉치(작업 로그·런북·노트)의 이식은 `/wiki-import` 스킬이 맡는다.
- 위키 변경 커밋은 장부 형식 `wiki: <동작> — <대상>: <요지>`를 따른다 (commit-msg 훅이 강제).

## 공용과 개인 — 요약

원본: `docs/harness-guide.md` (자산별 경로 표·승격 규칙).

- 커밋되는 모든 것은 전원의 세션에 로드된다. 판단 기준 한 문장: "전원의 세션에 있어야
  하는가?" (개인 모드에서는 "미래의 내 세션에 있어야 하는가") — 아니면 개인 영역에.
- 개인 경계는 위치가 아니라 **`my-` 접두사**다(.gitignore가 커밋을 막는다). 공용 자산에
  `my-` 이름 금지. **위키에 개인 영역은 없다** — 위키는 정의상 공용이다.

## 에이전트 문서 규칙

- **AGENTS.md가 원본**이고 `CLAUDE.md`는 symlink다. 내용 수정은 **반드시 AGENTS.md**에서.
- **반복 교정은 규칙으로 승격**: 에이전트가 같은 실수를 두 번 하면 그 자리에서 이 문서에 한 줄 추가를 제안한다.
- 지시 자산의 작성 기준(분량·역할 분담·심화 이관·스킬 저작)은 `docs/agents-guide.md` "문서 작성 기준"이 원본이다.

## 온보딩과 거버넌스 — 요약

- 온보딩(최초 1회, `scripts/setup.sh` → `/wiki-start`)과 운영 모드(개인/팀)·책임자(DRI)·거버넌스 규약은 `docs/harness-guide.md` "온보딩"·"거버넌스"가 원본이다.
